#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: the int to check
 * Return: the absolute value if int
 */
int_abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
